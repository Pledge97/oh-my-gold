# V3 Refactor Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement V3 so T 仓状态以 `signals` 为唯一数据源，底仓迁移到 `base_holdings`，并移除前后端对 `positions` / `position_lots` 的 T 仓依赖。

**Architecture:** 后端新增轻量仓位推导层，从 `signals` 重建当前轮次 T 仓状态，并让策略执行只写 `signals.pnl_yuan`。底仓改为独立 `base_holdings` API，前端删除独立 T 仓批次视图，把 T 仓摘要合并到信号面板。

**Tech Stack:** Python 3, FastAPI, SQLite, pytest, React 18, Zustand, Ant Design, TypeScript.

---

## Scope Check

本计划覆盖一个可独立交付的纵向切片：数据库 schema、T 仓状态推导、策略读写、API、前端展示。V3 迁移脚本在规格文档中标记“已完成”，因此本计划不实现历史数据迁移脚本，只调整运行时代码和测试，使新代码不再依赖旧表。

注意现有工作区已有用户改动：

- `frontend/src/App.tsx`
- `frontend/src/components/PortfolioView.tsx`

执行本计划时必须先读取这些文件的当前内容，保留用户改动，不要回滚。

## File Structure

- Modify: `backend/db/models.py`
  - 新增 `CREATE_BASE_HOLDINGS`
  - `signals` schema 增加 `pnl_yuan`
  - 移除 `CREATE_POSITIONS` / `CREATE_POSITION_LOTS` 的初始化依赖
  - `CREATE_INDEXES` 增加 `signals.ts`、`signals.type`、`base_holdings.status`
- Modify: `backend/db/database.py`
  - 初始化 `daily_prices`、`prices`、`signals`、`base_holdings`、`circuit_breaker_logs`
  - 对既有库执行幂等 schema 补齐：`signals.pnl_yuan`
  - 不再自动创建 `positions` / `position_lots`
- Create: `backend/risk/portfolio.py`
  - 定义 V3 `PortfolioPosition`
  - 定义信号类型常量
  - 从 `signals` 推导当前轮次、持仓量、均价、止盈标记、已实现盈亏、上次买入价
  - 提供 `buy()`、`sell()`、`clear()`、`pnl_pct()` 等内存操作
- Modify: `backend/risk/position.py`
  - 删除或停止使用 V2 `Lot` / `PortfolioPosition`
  - 保留 V1 `Position` / `PositionManager` 只在仍有测试覆盖时使用；若无生产引用，后续单独清理
- Modify: `backend/signals/buy_signal.py`
  - 基于 `portfolio.total_amount_g` 决定补仓目标：0→50g、<50→补至50g、<80→补至80g、<100→补至100g
  - 不再依赖 `portfolio.lot_count`
- Modify: `backend/strategy/engine.py`
  - 初始化时从 `signals` 恢复 V3 T 仓
  - 买入只写 `signals`
  - 卖出前先计算 `pnl_yuan`，再写 `signals.pnl_yuan`
  - 删除 `_save_position_*_v2`、`_save_lot_v2`、`_load_portfolio_v2`
  - WebSocket `portfolio` 删除 `lots`，保留 `round_counter`
- Modify: `backend/api/routes.py`
  - `GET /api/base_holdings`
  - `POST /api/base_holdings`
  - `POST /api/base_holdings/{id}/close`
  - `GET /api/performance` 改为从 `signals.pnl_yuan` 和 `base_holdings.pnl_yuan` 统计
  - 可短期保留旧 `/api/positions` 返回 410 或兼容代理，计划内优先直接迁移前端调用
- Modify: `frontend/src/types.ts`
  - `PortfolioPosition` 删除 `lots`，`round_id` 改为 `round_counter`
  - `Signal` 增加 `pnl_yuan?: number | null`
- Modify: `frontend/src/api/client.ts`
  - `fetchPositions` 改名或改实现为 `fetchBaseHoldings`
  - `createPosition` 改为 `createBaseHolding`
  - `closePosition` 改为 `closeBaseHolding`
- Modify: `frontend/src/store/useStore.ts`
  - `DbPosition` 改名为 `BaseHolding` 或保留类型名但字段对齐 `base_holdings`
  - 注释改为 V3
- Modify: `frontend/src/components/SignalPanel.tsx`
  - 标题下新增 T 仓摘要栏
  - 展示持仓、均价、浮盈、浮盈率、下次买入/止盈/止损
- Modify: `frontend/src/components/PositionTable.tsx`
  - 使用 `/api/base_holdings`
  - 删除 `source=manual`
  - 文案保持底仓语义
- Modify: `frontend/src/App.tsx`
  - 删除 `PortfolioView` import 和布局块
  - 右侧恢复两栏：上半 `SignalPanel`，下半 `PositionTable`
- Delete: `frontend/src/components/PortfolioView.tsx`
  - V3 不再展示批次列表
- Tests:
  - Create: `tests/test_db_v3.py`
  - Create: `tests/test_portfolio_v3.py`
  - Create: `tests/test_performance_v3.py`
  - Modify: `tests/test_buy_signal.py`
  - Modify: `tests/test_position.py`
  - Modify/remove: `tests/test_db_v2.py`

---

### Task 1: Database Schema V3

**Files:**
- Modify: `backend/db/models.py`
- Modify: `backend/db/database.py`
- Create: `tests/test_db_v3.py`
- Modify/remove: `tests/test_db_v2.py`

- [ ] **Step 1: Write failing schema tests**

Create `tests/test_db_v3.py`:

```python
import sqlite3
import tempfile
import os

from backend.db.database import init_db


def _new_db():
    tmp = tempfile.NamedTemporaryFile(suffix=".db", delete=False)
    tmp.close()
    return tmp.name


def test_base_holdings_table_exists_with_required_columns():
    """验证 V3 底仓表会在初始化时创建。"""
    db_path = _new_db()
    conn = None
    try:
        init_db(db_path)
        conn = sqlite3.connect(db_path)
        cols = {row[1] for row in conn.execute("PRAGMA table_info(base_holdings)").fetchall()}
        assert {
            "id", "open_ts", "open_price", "amount_g", "status",
            "close_ts", "close_price", "pnl_yuan",
        } <= cols
    finally:
        if conn:
            conn.close()
        os.unlink(db_path)


def test_signals_has_pnl_yuan_column():
    """验证 signals 支持记录每次卖出实现盈亏。"""
    db_path = _new_db()
    conn = None
    try:
        init_db(db_path)
        conn = sqlite3.connect(db_path)
        cols = {row[1] for row in conn.execute("PRAGMA table_info(signals)").fetchall()}
        assert "pnl_yuan" in cols
    finally:
        if conn:
            conn.close()
        os.unlink(db_path)


def test_v3_init_does_not_create_legacy_position_tables():
    """验证新库不再创建 V2 T 仓旧表。"""
    db_path = _new_db()
    conn = None
    try:
        init_db(db_path)
        conn = sqlite3.connect(db_path)
        tables = {
            row[0]
            for row in conn.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
        }
        assert "positions" not in tables
        assert "position_lots" not in tables
    finally:
        if conn:
            conn.close()
        os.unlink(db_path)
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
pytest tests/test_db_v3.py -v
```

Expected: FAIL because `base_holdings` and `signals.pnl_yuan` are not created, while legacy tables still are.

- [ ] **Step 3: Implement schema**

In `backend/db/models.py`, add:

```python
CREATE_BASE_HOLDINGS = """
CREATE TABLE IF NOT EXISTS base_holdings (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    open_ts     INTEGER NOT NULL,
    open_price  REAL NOT NULL,
    amount_g    REAL NOT NULL,
    status      TEXT NOT NULL DEFAULT 'OPEN',
    close_ts    INTEGER,
    close_price REAL,
    pnl_yuan    REAL
);
"""
```

Update `CREATE_SIGNALS` so it includes:

```sql
pnl_yuan REAL
```

Update `CREATE_INDEXES`:

```python
CREATE_INDEXES = """
CREATE INDEX IF NOT EXISTS idx_prices_ts ON prices(ts);
CREATE INDEX IF NOT EXISTS idx_signals_ts ON signals(ts);
CREATE INDEX IF NOT EXISTS idx_signals_type ON signals(type);
CREATE INDEX IF NOT EXISTS idx_base_holdings_status ON base_holdings(status);
"""
```

In `backend/db/database.py`, initialize only V3 tables:

```python
for sql in [
    models.CREATE_DAILY_PRICES,
    models.CREATE_PRICES,
    models.CREATE_SIGNALS,
    models.CREATE_BASE_HOLDINGS,
    models.CREATE_CIRCUIT_BREAKER_LOGS,
]:
    conn.execute(sql)
```

Add an idempotent helper:

```python
def _ensure_column(conn: sqlite3.Connection, table: str, column: str, ddl: str) -> None:
    """如果旧库缺少字段，则执行 ALTER TABLE 补齐。"""
    cols = {row["name"] for row in conn.execute(f"PRAGMA table_info({table})").fetchall()}
    if column not in cols:
        conn.execute(ddl)
```

Call it after table creation:

```python
_ensure_column(conn, "signals", "pnl_yuan", "ALTER TABLE signals ADD COLUMN pnl_yuan REAL")
```

- [ ] **Step 4: Remove V2 DB tests or rewrite them**

Replace `tests/test_db_v2.py` assertions with a short compatibility note test, or delete the file if no longer needed. Preferred: remove `tests/test_db_v2.py` because its expectations contradict V3.

- [ ] **Step 5: Run schema tests**

Run:

```bash
pytest tests/test_db_v3.py -v
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/db/models.py backend/db/database.py tests/test_db_v3.py tests/test_db_v2.py
git commit -m "refactor: add v3 database schema"
```

---

### Task 2: Portfolio State Derived From Signals

**Files:**
- Create: `backend/risk/portfolio.py`
- Modify: `backend/risk/position.py`
- Create: `tests/test_portfolio_v3.py`
- Modify: `tests/test_position.py`

- [ ] **Step 1: Write failing portfolio tests**

Create `tests/test_portfolio_v3.py`:

```python
import pytest

from backend.risk.portfolio import (
    PortfolioPosition,
    get_current_round_signals,
    load_portfolio_from_signals,
)


def insert_signal(conn, ts, sig_type, price, amount_g, pnl_yuan=None):
    conn.execute(
        "INSERT INTO signals (ts, type, mode, price, amount_g, reason, pnl_yuan) "
        "VALUES (?, ?, 'OSCILLATION', ?, ?, 'test', ?)",
        (ts, sig_type, price, amount_g, pnl_yuan),
    )


def test_current_round_starts_after_last_full_clear(tmp_path):
    """验证当前轮次只包含最近一次全清仓之后的信号。"""
    from backend.db.database import init_db, get_conn
    import backend.db.database as db_mod

    db_mod.DB_PATH = tmp_path / "test.db"
    init_db()
    with get_conn() as conn:
        insert_signal(conn, 1000, "BUY", 1000.0, 50.0)
        insert_signal(conn, 2000, "STOP_LOSS_CLEAR", 980.0, 50.0, -120.0)
        insert_signal(conn, 3000, "BUY", 996.0, 50.0)
        rows = get_current_round_signals(conn)
    assert [row["ts"] for row in rows] == [3000]


def test_load_portfolio_rebuilds_amount_cost_flags_and_pnl(tmp_path):
    """验证从 signals 可以恢复当前 T 仓状态。"""
    from backend.db.database import init_db, get_conn
    import backend.db.database as db_mod

    db_mod.DB_PATH = tmp_path / "test.db"
    init_db()
    with get_conn() as conn:
        insert_signal(conn, 1000, "BUY", 1000.0, 50.0)
        insert_signal(conn, 2000, "ADD_LOT", 990.0, 30.0)
        insert_signal(conn, 3000, "TAKE_PROFIT_1", 1010.0, 48.0, 600.0)
        portfolio = load_portfolio_from_signals(conn)

    assert portfolio.total_amount_g == pytest.approx(32.0)
    assert portfolio.realized_pnl == pytest.approx(600.0)
    assert portfolio.tp1_done is True
    assert portfolio.tp2_done is False
    assert portfolio.last_buy_price == pytest.approx(990.0)
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
pytest tests/test_portfolio_v3.py -v
```

Expected: FAIL because `backend.risk.portfolio` does not exist.

- [ ] **Step 3: Implement V3 portfolio module**

Create `backend/risk/portfolio.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from sqlite3 import Connection, Row

from backend import config

BUY_TYPES = ("BUY", "ADD_LOT")
PARTIAL_SELL_TYPES = ("TAKE_PROFIT_1", "TAKE_PROFIT_2", "STOP_LOSS_HALF")
FULL_CLEAR_TYPES = ("STOP_LOSS_CLEAR", "TREND_CLEAR", "TAKE_PROFIT_TRAILING")
SELL_TYPES = PARTIAL_SELL_TYPES + FULL_CLEAR_TYPES


@dataclass
class PortfolioPosition:
    """V3 T仓内存状态，所有字段均可从 signals 推导。"""
    round_counter: int = 0
    total_amount_g: float = 0.0
    total_cost: float = 0.0
    tp1_done: bool = False
    tp2_done: bool = False
    realized_pnl: float = 0.0
    last_buy_price: float | None = None

    @property
    def avg_cost(self) -> float:
        """返回当前持仓均价。"""
        if self.total_amount_g == 0:
            return 0.0
        return self.total_cost / self.total_amount_g

    def is_empty(self) -> bool:
        """判断当前 T 仓是否为空。"""
        return self.total_amount_g <= 0

    def pnl_pct(self, current_price: float) -> float:
        """计算扣除卖出手续费后的浮动盈亏率。"""
        if self.total_cost == 0:
            return 0.0
        market_value = current_price * self.total_amount_g
        fee = market_value * config.SELL_FEE_RATE
        return (market_value - fee - self.total_cost) / self.total_cost

    def buy(self, price: float, amount_g: float) -> None:
        """记录买入或加仓后的内存状态。"""
        self.total_amount_g += amount_g
        self.total_cost += price * amount_g
        self.last_buy_price = price

    def sell(self, price: float, amount_g: float) -> float:
        """按当前均价卖出指定克数，返回本次实现盈亏。"""
        if self.total_amount_g <= 0:
            return 0.0
        sold_g = min(amount_g, self.total_amount_g)
        avg_cost = self.avg_cost
        pnl_yuan = calc_sell_pnl(sold_g, price, avg_cost, config.SELL_FEE_RATE)
        cost_removed = avg_cost * sold_g
        self.total_amount_g = round(self.total_amount_g - sold_g, 4)
        self.total_cost = round(max(self.total_cost - cost_removed, 0.0), 4)
        self.realized_pnl = round(self.realized_pnl + pnl_yuan, 4)
        return pnl_yuan


def calc_sell_pnl(sold_g: float, sell_price: float, avg_cost: float, fee_rate: float) -> float:
    """计算单次卖出已实现盈亏。"""
    revenue = sell_price * sold_g
    cost = avg_cost * sold_g
    fee = revenue * fee_rate
    return revenue - cost - fee


def get_current_round_signals(conn: Connection) -> list[Row]:
    """读取最近一次全清仓之后的当前轮次信号。"""
    last_clear = conn.execute(
        "SELECT MAX(ts) ts FROM signals WHERE type IN (?,?,?)",
        FULL_CLEAR_TYPES,
    ).fetchone()
    since_ts = last_clear["ts"] if last_clear and last_clear["ts"] is not None else 0
    return conn.execute(
        "SELECT * FROM signals WHERE ts > ? ORDER BY ts ASC",
        (since_ts,),
    ).fetchall()


def load_portfolio_from_signals(conn: Connection) -> PortfolioPosition:
    """从当前轮次 signals 重建 V3 T 仓状态。"""
    rows = get_current_round_signals(conn)
    portfolio = PortfolioPosition()
    for row in rows:
        sig_type = row["type"]
        amount_g = float(row["amount_g"])
        price = float(row["price"])
        if sig_type in BUY_TYPES:
            portfolio.buy(price, amount_g)
        elif sig_type in SELL_TYPES:
            # 回放时不调用 sell()（会重新计算盈亏），直接用存储的 pnl_yuan
            sold_g = min(amount_g, portfolio.total_amount_g)
            avg = portfolio.avg_cost
            portfolio.total_amount_g = round(portfolio.total_amount_g - sold_g, 4)
            portfolio.total_cost = round(max(portfolio.total_cost - avg * sold_g, 0.0), 4)
            if row["pnl_yuan"] is not None:
                portfolio.realized_pnl = round(portfolio.realized_pnl + float(row["pnl_yuan"]), 4)
        if sig_type == "TAKE_PROFIT_1":
            portfolio.tp1_done = True
        if sig_type == "TAKE_PROFIT_2":
            portfolio.tp2_done = True
    return portfolio
```

- [ ] **Step 4: Update existing tests**

In `tests/test_position.py`, remove V2 `PortfolioPosition` tests or migrate imports to `backend.risk.portfolio.PortfolioPosition` and replace `add_lot()` calls with `buy()`.

In `tests/test_buy_signal.py`, update the import at the top and fix all existing tests that use the old V2 API:

```python
# 替换旧导入
from backend.risk.portfolio import PortfolioPosition  # 替换 from backend.risk.position import PortfolioPosition

# 替换 empty_pos()
def empty_pos():
    return PortfolioPosition()  # 不再需要 round_id 参数

# 替换所有 pos.add_lot(index, price, g, ts) 调用为 pos.buy(price, g)
# 例如：
# 旧: pos.add_lot(1, 1000.0, LOT1_AMOUNT_G, 1000)
# 新: pos.buy(1000.0, LOT1_AMOUNT_G)
```

- [ ] **Step 5: Run portfolio tests**

Run:

```bash
pytest tests/test_portfolio_v3.py tests/test_position.py -v
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/risk/portfolio.py backend/risk/position.py tests/test_portfolio_v3.py tests/test_position.py
git commit -m "refactor: derive portfolio from signals"
```

---

### Task 3: Refactor Buy Logic To Amount Thresholds

**Files:**
- Modify: `backend/signals/buy_signal.py`
- Modify: `tests/test_buy_signal.py`

- [ ] **Step 1: Write failing amount-threshold tests**

In `tests/test_buy_signal.py`, update helpers to use V3 portfolio:

```python
from backend.risk.portfolio import PortfolioPosition
```

Add tests:

```python
def test_add_refills_partial_position_to_50g():
    """持仓低于50g时，加仓补到50g。"""
    pos = PortfolioPosition()
    pos.buy(1000.0, 20.0)
    ctx = make_context(price=994.0, bb_lower=1001.0, atr_5m=5.0)
    signal = check_buy_signal(ctx, pos, circuit_breaker_active=False, last_buy_price=1000.0)
    assert signal is not None
    assert signal.amount_g == pytest.approx(30.0)


def test_add_refills_50_to_80g():
    """持仓50g到80g之间时，加仓补到80g。"""
    pos = PortfolioPosition()
    pos.buy(1000.0, 50.0)
    ctx = make_context(price=994.0, bb_lower=1001.0, atr_5m=5.0)
    signal = check_buy_signal(ctx, pos, circuit_breaker_active=False, last_buy_price=1000.0)
    assert signal is not None
    assert signal.amount_g == pytest.approx(30.0)


def test_add_refills_80_to_100g():
    """持仓80g到100g之间时，加仓补到100g。"""
    pos = PortfolioPosition()
    pos.buy(1000.0, 80.0)
    ctx = make_context(price=994.0, bb_lower=1001.0, atr_5m=5.0)
    signal = check_buy_signal(ctx, pos, circuit_breaker_active=False, last_buy_price=1000.0)
    assert signal is not None
    assert signal.amount_g == pytest.approx(20.0)
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
pytest tests/test_buy_signal.py -v
```

Expected: FAIL where code still uses `lot_count`.

- [ ] **Step 3: Implement amount target helper**

In `backend/signals/buy_signal.py`, add constants or helper:

```python
def _next_target_amount_g(current_amount_g: float) -> float | None:
    """根据当前持仓克数返回下一档目标持仓。"""
    if current_amount_g <= 0:
        return config.LOT1_AMOUNT_G
    if current_amount_g < config.LOT1_AMOUNT_G:
        return config.LOT1_AMOUNT_G
    if current_amount_g < config.LOT1_AMOUNT_G + config.LOT2_AMOUNT_G:
        return config.LOT1_AMOUNT_G + config.LOT2_AMOUNT_G
    if current_amount_g < config.T_MAX_AMOUNT_G:
        return config.T_MAX_AMOUNT_G
    return None
```

Use it in add logic:

```python
target_amount_g = _next_target_amount_g(portfolio.total_amount_g)
if target_amount_g is None:
    return None
amount_g = round(target_amount_g - portfolio.total_amount_g, 4)
```

- [ ] **Step 4: Run buy signal tests**

Run:

```bash
pytest tests/test_buy_signal.py -v
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/signals/buy_signal.py tests/test_buy_signal.py
git commit -m "refactor: base add logic on holding amount"
```

---

### Task 4: Strategy Engine Writes Only Signals For T 仓

**Files:**
- Modify: `backend/strategy/engine.py`
- Modify: `tests/test_sell_signal.py`
- Modify: `tests/test_exit_signal.py`
- Create: `tests/test_strategy_v3.py`

- [ ] **Step 1: Write failing strategy tests**

Create `tests/test_strategy_v3.py`:

```python
import pytest

from backend.core.enums import MarketState
from backend.db.database import init_db, get_conn
from backend.risk.portfolio import PortfolioPosition
from backend.strategy.engine import StrategyEngine


@pytest.fixture(autouse=True)
def clean_db(tmp_path, monkeypatch):
    import backend.db.database as db_mod
    monkeypatch.setattr(db_mod, "DB_PATH", tmp_path / "test.db")
    init_db()


def test_save_sell_signal_persists_pnl_yuan():
    """验证卖出信号会写入本次实现盈亏。"""
    engine = StrategyEngine()
    with get_conn() as conn:
        engine._save_signal_raw(
            conn=conn,
            ts=1000,
            sig_type="TAKE_PROFIT_1",
            mode=MarketState.OSCILLATION.value,
            price=1010.0,
            amount_g=50.0,
            reason="test",
            pnl_yuan=298.0,
        )
        row = conn.execute("SELECT pnl_yuan FROM signals WHERE type='TAKE_PROFIT_1'").fetchone()
    assert row["pnl_yuan"] == pytest.approx(298.0)


def test_portfolio_snapshot_has_no_lots_field():
    """验证 WebSocket portfolio 不再返回 lots。"""
    engine = StrategyEngine()
    engine._portfolio = PortfolioPosition(round_counter=3)
    engine._portfolio.buy(1000.0, 50.0)
    ctx = type("Ctx", (), {"indicators": type("I", (), {"atr_5m": 5.0, "bb_lower": 990.0})()})()
    snapshot = engine._portfolio_snapshot(1010.0, engine._portfolio.pnl_pct(1010.0), ctx)
    assert snapshot["round_counter"] == 3
    assert "lots" not in snapshot
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
pytest tests/test_strategy_v3.py -v
```

Expected: FAIL because `_save_signal_raw` does not exist and snapshot still includes `lots`.

- [ ] **Step 3: Refactor engine initialization**

In `backend/strategy/engine.py`, import:

```python
from backend.risk.portfolio import PortfolioPosition, load_portfolio_from_signals, calc_sell_pnl
```

Replace V2 state:

```python
self._portfolio: PortfolioPosition = PortfolioPosition()
self._v3_round_counter: int = 0
self._load_portfolio_v3()
```

Implement:

```python
def _load_portfolio_v3(self) -> None:
    """服务启动时从 signals 恢复 V3 T仓状态。"""
    with get_conn() as conn:
        self._portfolio = load_portfolio_from_signals(conn)
        row = conn.execute(
            "SELECT COUNT(*) cnt FROM signals WHERE type IN (?,?,?)",
            ("STOP_LOSS_CLEAR", "TREND_CLEAR", "TAKE_PROFIT_TRAILING"),
        ).fetchone()
        self._portfolio.round_counter = row["cnt"] or 0
```

- [ ] **Step 4: Refactor signal persistence**

Replace `_save_signal()` with:

```python
def _save_signal(self, ctx: MarketContext, sig_type: str, amount_g: float,
                 reason: str, pnl_yuan: float | None = None) -> None:
    """保存策略信号，卖出信号可记录本次已实现盈亏。"""
    with get_conn() as conn:
        self._save_signal_raw(
            conn=conn,
            ts=ctx.ts or int(time.time() * 1000),
            sig_type=sig_type,
            mode=ctx.market_state.value,
            price=ctx.price,
            amount_g=amount_g,
            reason=reason,
            pnl_yuan=pnl_yuan,
        )


def _save_signal_raw(self, conn, ts: int, sig_type: str, mode: str, price: float,
                     amount_g: float, reason: str, pnl_yuan: float | None = None) -> None:
    """底层信号写入方法，方便测试直接验证。"""
    conn.execute(
        "INSERT INTO signals (ts, type, mode, price, amount_g, reason, pnl_yuan) "
        "VALUES (?, ?, ?, ?, ?, ?, ?)",
        (ts, sig_type, mode, price, amount_g, reason, pnl_yuan),
    )
```

- [ ] **Step 5: Refactor buy/sell/exit execution**

In `_execute_buy_v2()`:

- Rename to `_execute_buy_v3()` or keep name until all call sites are updated.
- Remove `_save_position_open_v2()`, `_save_lot_v2()`, `_update_position_amount_v2()`.
- Use `self._portfolio.buy(ctx.price, signal.amount_g)`.
- Set `self._portfolio.round_counter += 1` only when buying from empty.

In `_execute_sell_v2()` and `_execute_exit_v2()`:

- Capture `avg_cost = self._portfolio.avg_cost` before reducing.
- Determine `sold_g`.
- Compute `pnl_yuan = calc_sell_pnl(sold_g, ctx.price, avg_cost, config.SELL_FEE_RATE)`.
- Update in-memory portfolio with `self._portfolio.sell(ctx.price, sold_g)`.
- Save signal with `pnl_yuan=round(pnl_yuan, 2)`.
- If full clear, increment round boundary only after next new buy; do not write `positions`.

- [ ] **Step 6: Refactor snapshot**

Return:

```python
# 下次买入触发价：空仓用布林下轨，有仓用 last_buy_price - ATR 间距
if not has_position:
    next_buy = ctx.indicators.bb_lower or None
elif self._portfolio.total_amount_g < config.T_MAX_AMOUNT_G and atr and self._portfolio.last_buy_price:
    next_buy = round(self._portfolio.last_buy_price - config.ATR_ADD_LOT_MULTIPLIER * atr, 2)
else:
    next_buy = None  # 满仓

return {
    "round_counter": self._portfolio.round_counter,
    "total_amount_g": self._portfolio.total_amount_g,
    "avg_cost": avg_cost,
    "pnl_pct": pnl_pct,
    "pnl_yuan": round(price * self._portfolio.total_amount_g * (1 - config.SELL_FEE_RATE) - self._portfolio.total_cost, 2),
    "tp1_done": self._portfolio.tp1_done,
    "tp2_done": self._portfolio.tp2_done,
    "next_buy": next_buy,
    "next_tp": next_tp,
    "next_stop": next_stop,
}
```

- [ ] **Step 7: Run strategy and signal tests**

Run:

```bash
pytest tests/test_strategy_v3.py tests/test_sell_signal.py tests/test_exit_signal.py -v
```

Expected: PASS.

- [ ] **Step 8: Commit**

```bash
git add backend/strategy/engine.py tests/test_strategy_v3.py tests/test_sell_signal.py tests/test_exit_signal.py
git commit -m "refactor: persist t position state through signals"
```

---

### Task 5: Base Holdings API And Performance

**Files:**
- Modify: `backend/api/routes.py`
- Create: `tests/test_performance_v3.py`

- [ ] **Step 1: Write failing API/performance tests**

Create `tests/test_performance_v3.py`:

```python
import pytest
from fastapi.testclient import TestClient

from backend.db.database import init_db, get_conn
from backend.main import app


@pytest.fixture(autouse=True)
def clean_db(tmp_path, monkeypatch):
    import backend.db.database as db_mod
    monkeypatch.setattr(db_mod, "DB_PATH", tmp_path / "test.db")
    init_db()


def test_base_holdings_create_list_and_close():
    """验证底仓 API 可以创建、查询、平仓。"""
    client = TestClient(app)
    created = client.post(
        "/api/base_holdings",
        json={"amount_g": 10.0, "open_price": 990.0, "open_date": "2026-05-18"},
    )
    assert created.status_code == 200
    pos_id = created.json()["id"]

    listed = client.get("/api/base_holdings?status=OPEN")
    assert listed.status_code == 200
    assert len(listed.json()) == 1

    closed = client.post(
        f"/api/base_holdings/{pos_id}/close",
        json={"close_price": 1000.0, "close_date": "2026-05-18 10:30"},
    )
    assert closed.status_code == 200
    assert closed.json()["status"] == "CLOSED"


def test_performance_uses_signals_and_base_holdings():
    """验证绩效统计来自 signals.pnl_yuan 和 base_holdings.pnl_yuan。"""
    with get_conn() as conn:
        conn.execute(
            "INSERT INTO signals (ts, type, mode, price, amount_g, reason, pnl_yuan) "
            "VALUES (1, 'TAKE_PROFIT_1', 'OSCILLATION', 1010, 50, 'test', 298)"
        )
        conn.execute(
            "INSERT INTO base_holdings (open_ts, open_price, amount_g, status, close_ts, close_price, pnl_yuan) "
            "VALUES (1, 990, 10, 'CLOSED', 2, 1000, 60)"
        )

    client = TestClient(app)
    data = client.get("/api/performance").json()
    assert data["total_trades"] == 1
    assert data["total_pnl_yuan"] == pytest.approx(298.0)
    assert data["cumulative_pnl_yuan"] == pytest.approx(358.0)
```

- [ ] **Step 2: Run tests to verify failure**

Run:

```bash
pytest tests/test_performance_v3.py -v
```

Expected: FAIL because `/api/base_holdings` does not exist and performance still reads `positions`.

- [ ] **Step 3: Implement base holdings API**

In `backend/api/routes.py`, rename request models or add V3 models:

```python
class BaseHoldingIn(BaseModel):
    amount_g: float
    open_price: float
    open_date: str


class CloseBaseHoldingIn(BaseModel):
    close_price: float
    close_date: str
```

Add routes:

```python
@router.get("/base_holdings")
def get_base_holdings(status: str = "OPEN"):
    """查询底仓列表。"""
    with get_conn() as conn:
        rows = conn.execute(
            "SELECT * FROM base_holdings WHERE status=? ORDER BY open_ts DESC",
            (status,),
        ).fetchall()
    return [dict(row) for row in rows]
```

For close:

```python
fee = body.close_price * row["amount_g"] * 0.004
pnl_yuan = (body.close_price - row["open_price"]) * row["amount_g"] - fee
```

Update table:

```sql
UPDATE base_holdings
SET status='CLOSED', close_ts=?, close_price=?, pnl_yuan=?
WHERE id=?
```

- [ ] **Step 4: Refactor performance**

Use sell types:

```python
sell_types = (
    "TAKE_PROFIT_1", "TAKE_PROFIT_2", "TAKE_PROFIT_TRAILING",
    "STOP_LOSS_HALF", "STOP_LOSS_CLEAR", "TREND_CLEAR",
)
```

Calculate:

- `total_trades`: count sell signals
- `total_pnl_yuan`: `SUM(pnl_yuan)` from sell signals
- `cumulative_pnl_yuan`: T 仓盈亏 + `SUM(pnl_yuan)` from `base_holdings WHERE status='CLOSED'`
- `win_rate`: Python 层按轮次分组计算； first pass can treat each full-clear bounded group as a round and include open/current partial sells as incomplete round only if it has at least one sell.

- [ ] **Step 5: Run API tests**

Run:

```bash
pytest tests/test_performance_v3.py -v
```

Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/api/routes.py tests/test_performance_v3.py
git commit -m "feat: add base holdings api"
```

---

### Task 6: Frontend Type And API Migration

**Files:**
- Modify: `frontend/src/types.ts`
- Modify: `frontend/src/api/client.ts`
- Modify: `frontend/src/store/useStore.ts`

- [ ] **Step 1: Update frontend types**

In `frontend/src/types.ts`:

```ts
export interface PortfolioPosition {
  round_counter: number
  total_amount_g: number
  avg_cost: number
  pnl_pct: number
  pnl_yuan: number
  tp1_done: boolean
  tp2_done: boolean
  next_buy: number | null
  next_tp: number | null
  next_stop: number | null
}

export interface Signal {
  id: number
  ts: number
  type: string
  mode: string
  price: number
  amount_g: number
  reason: string
  pnl_yuan: number | null
}
```

- [ ] **Step 2: Update API client**

In `frontend/src/api/client.ts`, replace position calls:

```ts
export const fetchBaseHoldings = (status = 'OPEN') =>
  fetch(`${BASE}/base_holdings?status=${status}`).then(r => r.json())

export const createBaseHolding = (body: { amount_g: number; open_price: number; open_date: string }) =>
  fetch(`${BASE}/base_holdings`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  }).then(r => r.json())

export const closeBaseHolding = (posId: number, body: { close_price: number; close_date: string }) =>
  fetch(`${BASE}/base_holdings/${posId}/close`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(body),
  }).then(r => r.json())
```

- [ ] **Step 3: Update Zustand store**

In `frontend/src/store/useStore.ts`, rename or alias:

```ts
export interface BaseHolding {
  id: number
  open_ts: number
  open_price: number
  amount_g: number
  status: string
  pnl_yuan: number | null
}
```

Keep state key `dbPositions` only if minimizing UI churn. Preferred if touching PositionTable anyway:

```ts
baseHoldings: BaseHolding[]
setBaseHoldings: (p: BaseHolding[]) => void
```

- [ ] **Step 4: Run lightweight frontend type check**

Run from `frontend`:

```bash
npm exec tsc -- --noEmit
```

Expected: FAIL until component imports are migrated.

- [ ] **Step 5: Commit**

```bash
git add frontend/src/types.ts frontend/src/api/client.ts frontend/src/store/useStore.ts
git commit -m "refactor: migrate frontend types to v3 portfolio"
```

---

### Task 7: Frontend UI Migration

**Files:**
- Modify: `frontend/src/components/SignalPanel.tsx`
- Modify: `frontend/src/components/PositionTable.tsx`
- Modify: `frontend/src/App.tsx`
- Delete: `frontend/src/components/PortfolioView.tsx`

- [ ] **Step 1: Move T 仓 summary into SignalPanel**

In `SignalPanel.tsx`, derive:

```ts
const amountG = portfolio?.total_amount_g ?? 0
const avgCost = portfolio?.avg_cost ?? 0
const pnlYuan = portfolio?.pnl_yuan ?? 0
const pnlPct = portfolio?.pnl_pct ?? 0
const pnlColor = pnlYuan >= 0 ? '#ff4d4f' : '#00ff88'
```

Below the title row and above the table body, render:

```tsx
<div style={{ padding: '6px 10px', borderTop: '1px solid #102a44', borderBottom: '1px solid #102a44', display: 'flex', gap: 12, fontSize: 11 }}>
  <span><span style={{ color: '#4a6a8a' }}>持仓 </span>{amountG.toFixed(1)}g</span>
  <span><span style={{ color: '#4a6a8a' }}>均价 </span>¥{avgCost.toFixed(2)}</span>
  <span style={{ color: pnlColor }}>
    <span style={{ color: '#4a6a8a' }}>浮盈 </span>
    {pnlYuan >= 0 ? '+' : ''}{pnlYuan.toFixed(2)}
    <span style={{ marginLeft: 4 }}>({pnlPct >= 0 ? '+' : ''}{(pnlPct * 100).toFixed(2)}%)</span>
  </span>
</div>
```

- [ ] **Step 2: Update PositionTable to base holdings**

Change imports:

```ts
import { createBaseHolding, closeBaseHolding, fetchBaseHoldings } from '../api/client'
import { useStore, BaseHolding } from '../store/useStore'
```

Change reload:

```ts
const reload = () => fetchBaseHoldings('OPEN').then(setBaseHoldings)
```

Replace `DbPosition` with `BaseHolding`, and replace `createPosition` / `closePosition` calls.

- [ ] **Step 3: Remove PortfolioView from App**

In `App.tsx`:

- Remove `import { PortfolioView } ...`
- Remove the `PortfolioView` panel block
- Change right side to:

```tsx
<div style={{ display: 'flex', flexDirection: 'column', gap: 12, minHeight: 0 }}>
  <div style={{ flex: 1, minHeight: 0, overflow: 'hidden', display: 'flex', flexDirection: 'column' }}>
    <SignalPanel />
  </div>
  <div style={{ flex: 1, minHeight: 0, overflow: 'hidden', display: 'flex', flexDirection: 'column' }}>
    <PositionTable />
  </div>
</div>
```

- [ ] **Step 4: Delete old component**

Delete:

```bash
git rm frontend/src/components/PortfolioView.tsx
```

- [ ] **Step 5: Run lightweight frontend type check**

Run from `frontend`:

```bash
npm exec tsc -- --noEmit
```

Expected: PASS. Do not run `npm run build` unless explicitly requested.

- [ ] **Step 6: Commit**

```bash
git add frontend/src/components/SignalPanel.tsx frontend/src/components/PositionTable.tsx frontend/src/App.tsx frontend/src/components/PortfolioView.tsx
git commit -m "refactor: show v3 portfolio in signal panel"
```

---

### Task 8: End-To-End Verification

**Files:**
- Potentially modify any file touched above if verification exposes integration issues.

- [ ] **Step 1: Run targeted backend tests**

Run:

```bash
pytest tests/test_db_v3.py tests/test_portfolio_v3.py tests/test_buy_signal.py tests/test_sell_signal.py tests/test_exit_signal.py tests/test_strategy_v3.py tests/test_performance_v3.py -v
```

Expected: PASS.

- [ ] **Step 2: Run full backend test suite**

Run:

```bash
pytest -v
```

Expected: PASS. If old V1/V2 tests fail because they assert removed behavior, update or delete those tests only after confirming the behavior is superseded by V3.

- [ ] **Step 3: Run frontend type check**

Run:

```bash
cd frontend
npm exec tsc -- --noEmit
```

Expected: PASS. Do not run full frontend build.

- [ ] **Step 4: Manual local smoke test**

Start backend:

```bash
uvicorn backend.main:app --reload --port 8000
```

In another shell:

```bash
curl http://127.0.0.1:8000/api/base_holdings
curl http://127.0.0.1:8000/api/performance
```

Expected:

- `/api/base_holdings` returns a JSON array.
- `/api/performance` returns numeric fields without querying missing `positions` tables.

- [ ] **Step 5: Inspect for legacy T 仓 dependencies**

Run:

```bash
rg -n "position_lots|/positions|PortfolioView|\\.lots|round_id|_load_portfolio_v2|_save_position|_save_lot" backend frontend/src tests
```

Expected:

- No `position_lots` runtime dependency.
- No frontend `/positions` calls.
- No `PortfolioView` import.
- `round_id` only appears in historical docs or intentionally retained V1 tests, not V3 WebSocket payload.

- [ ] **Step 6: Final commit**

```bash
git add .
git commit -m "refactor: complete v3 signal-sourced t position"
```

---

## Risks And Follow-Ups

- SQLite existing production databases may still contain `positions` and `position_lots`. V3 runtime should ignore them, but operational cleanup should be handled separately after backup.
- `backend/risk/position.py` still contains V1 `PositionManager`; if no runtime path uses `on_tick()` anymore, remove it in a later cleanup PR.
- The spec says migration is already complete. If deploying to a new environment with old data, verify `signals.pnl_yuan` and `base_holdings` data before deleting old tables.
- Frontend text currently has mojibake in several files when viewed from this Windows shell. Preserve actual file encoding and do not rewrite unrelated copy while implementing V3.
